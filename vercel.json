{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/webhook",
      "methods": ["POST"],
      "headers": {
        "content-type": "application/json"
      },
      "dest": "/webhook",
      "bodyParser": false
    },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
