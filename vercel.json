{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/webhook",
      "methods": ["POST"],
      "dest": "/webhook"
    },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
